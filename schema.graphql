type Issuance @entity {
  "USDT tokens issuance tx hash"
  id: ID!
  "Issuance amount"
  amount: BigInt!
}

type TotalIssuance @entity {
  "Only 1 id in existence"
  id: ID!

  "Issuance amount"
  amount: BigInt!
}

type Redemption @entity {
  "USDT tokens redeem tx hash"
  id: ID!
  "Redeem amount"
  amount: BigInt!
}


type TotalRedemption @entity {
  "Only 1 id in existence"
  id: ID!

  "Issuance amount"
  amount: BigInt!
}