type Issuance @entity {
  "USDT tokens issuance tx hash"
  id: ID!
  "Issuance amount"
  amount: BigInt!
}

type Redemption @entity {
  "USDT tokens redeem tx hash"
  id: ID!
  "Redeem amount"
  amount: BigInt!
}