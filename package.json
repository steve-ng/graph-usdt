{
  "name": "graph-usdt",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ steve-ng/graph-usdt",
    "create-local": "graph create --node http://localhost:8020/ steve-ng/graph-usdt",
    "remove-local": "graph remove --node http://localhost:8020/ steve-ng/graph-usdt",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 steve-ng/graph-usdt"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
